# ========================================
# SAP AI Assistant - Environment Configuration
# ========================================

# Application Settings
# --------------------
APP_ENV=dev                              # Environment: dev, staging, prod
APP_PORT=8000                            # Server port (default: 8000)

# AI Model Configuration
# ----------------------
# Optional: Leave empty or use "test_api_key_placeholder" for mock mode
# For real AI, use your OpenAI or Anthropic API key
MODEL_API_KEY=                           # Your AI provider API key

# Database Configuration (Optional)
# ---------------------------------
# Only needed if using database features
DB_ENGINE=postgres                       # Database engine: postgres, mysql, sqlite
DB_HOST=localhost                        # Database host
DB_PORT=5432                             # Database port
DB_NAME=sap_ai_assistant                 # Database name
DB_USER=                                 # Database username
DB_PASSWORD=                             # Database password

# ========================================
# Notes:
# - Copy this file to .env and fill in your values
# - Application works without AI API key (uses mock mode)
# - Database settings are optional for basic usage
# - Never commit .env file to version control
# ========================================
